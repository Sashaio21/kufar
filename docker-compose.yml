services:
  kufar-monitor:
    build: .
    container_name: kufar-monitor
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - kufar_data:/app/data

volumes:
  kufar_data:
